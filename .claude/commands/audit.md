You goal is to updated any vulanerable dependencies.

Do the following:
1. Run `npm audit` to find vulnerable installed packages in this project
2. Run `num audit fix` to apply updates
3. Run test and verify the updates didn't break anything